## 1. 长期入口与目录生成

- [x] 1.1 为 `init_daily_reports` 增加 `vault/index.md` 与 `vault/knowledge/index.md` 的缺失创建逻辑，保持已有文件不覆盖。
- [x] 1.2 为 `themes/events/evidence/reviews` 四个长期目录增加 `index.md` 缺失创建逻辑，入口名遵循 design 的中文固定 wikilink。
- [x] 1.3 增加长期入口模板文件，包含 YAML frontmatter、tags、一级标题和用途骨架。

## 2. 每日输出与模板增强

- [x] 2.1 增强每日入口页模板，使其链接 `[[知识库入口]]` 且不携带路径前缀。
- [x] 2.2 增强个股日评模板，补充证据来源、后续验证点或复盘入口，保留既有 14 节结构和报告边界提示。
- [x] 2.3 增强研究日志模板，使其承接当日验证点、待跟踪问题或后续观察事项。
- [x] 2.4 增强公司档案模板，补充长期跟踪入口链接，不重排稳定信息章节。

## 3. 行为验证与测试更新

- [x] 3.1 更新初始化测试，覆盖 `vault/index.md`、`vault/knowledge/**/index.md` 和既有公司/日期入口同时生成。
- [x] 3.2 更新模板渲染测试，覆盖中文固定 wikilink、无路径前缀、frontmatter/tags 和关键章节占位。
- [x] 3.3 更新幂等测试，确认已有入口页、公司页、日评和每日综合文档不会被覆盖或自动 patch。
- [x] 3.4 更新报告边界测试，确认模板不生成具体买卖价位、止盈止损或仓位建议字段。

## 4. 验证与收尾

- [x] 4.1 运行 `uv run pytest`，确认现有测试基线和新增测试通过。
- [x] 4.2 自检未新增外部数据源、自动主题发现、AI 自动摘要或交易建议能力。
- [x] 4.3 自检历史文件迁移策略符合 design：只创建缺失入口，不覆盖、不删除、不自动插入已有内容。
